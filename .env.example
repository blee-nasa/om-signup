DATABASE_URL=postgres://om_signup:om_signup@localhost:5438/om_signup
PORT=3100

# Optional. If set, the chat assistant and the health probe use the Anthropic API.
# If unset in local dev, they fall back to the local Claude CLI instead.
ANTHROPIC_API_KEY=
